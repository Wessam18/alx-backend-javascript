export default function updateStudentGradeByCity(students, city, newgrades) {
    return students
    .filter((students) => students.location === city)
    .map(function(students, newgrades) {

     })
}
